<!DOCTYPE doc [
<!ENTITY test1 "<a/>,<b/>,<c/>,<d/>">
]>
<doc>
   <ent>&test1;</ent>
   <p> WE need lot of garbage now to trigger the problem</p>
   <p> WE need lot of garbage now to trigger the problem</p>
   <p> WE need lot of garbage now to trigger the problem</p>
   <p> WE need lot of garbage now to trigger the problem</p>
   <p> WE need lot of garbage now to trigger the problem</p>
   <p> WE need lot of garbage now to trigger the problem</p>
   <p> WE need lot of garbage now to trigger the problem</p>
   <p> WE need lot of garbage now to trigger the problem</p>
   <p> WE need lot of garbage now to trigger the problem</p>
   <p> WE need lot of garbage now to trigger the problem</p>
   <p> WE need lot of garbage now to trigger the problem</p>
   <p> WE need lot of garbage now to trigger the problem</p>
   <p> WE need lot of garbage now to trigger the problem</p>
   <p> WE need lot of garbage now to trigger the problem</p>
   <p> WE need lot of garbage now to trigger the problem</p>
   <p> WE need lot of garbage now to trigger the problem</p>
   <p> WE need lot of garbage now to trigger the problem</p>
   <p> WE need lot of garbage now to trigger the problem</p>
   <p> WE need lot of garbage now to trigger the problem</p>
   <p> WE need lot of garbage now to trigger the problem</p>
   <p> WE need lot of garbage now to trigger the problem</p>
   <p> WE need lot of garbage now to trigger the problem</p>
   <p> WE need lot of garbage now to trigger the problem</p>
   <p> WE need lot of garbage now to trigger the problem</p>
   <p> WE need lot of garbage now to trigger the problem</p>
   <p> WE need lot of garbage now to trigger the problem</p>
   <p> WE need lot of garbage now to trigger the problem</p>
   <p> WE need lot of garbage now to trigger the problem</p>
   <p> WE need lot of garbage now to trigger the problem</p>
   <p> WE need lot of garbage now to trigger the problem</p>
   <p> WE need lot of garbage now to trigger the problem</p>
   <p> WE need lot of garbage now to trigger the problem</p>
   <p> WE need lot of garbage now to trigger the problem</p>
   <p> WE need lot of garbage now to trigger the problem</p>
   <p> WE need lot of garbage now to trigger the problem</p>
   <p> WE need lot of garbage now to trigger the problem</p>
   <p> WE need lot of garbage now to trigger the problem</p>
   <p> WE need lot of garbage now to trigger the problem</p>
   <p> WE need lot of garbage now to trigger the problem</p>
   <p> WE need lot of garbage now to trigger the problem</p>
   <p> WE need lot of garbage now to trigger the problem</p>
   <p> WE need lot of garbage now to trigger the problem</p>
   <p> WE need lot of garbage now to trigger the problem</p>
   <p> WE need lot of garbage now to trigger the problem</p>
   <p> WE need lot of garbage now to trigger the problem</p>
   <p> WE need lot of garbage now to trigger the problem</p>
   <p> WE need lot of garbage now to trigger the problem</p>
   <p> WE need lot of garbage now to trigger the problem</p>
   <p> WE need lot of garbage now to trigger the problem</p>
   <p> WE need lot of garbage now to trigger the problem</p>
   <p> WE need lot of garbage now to trigger the problem</p>
   <p> WE need lot of garbage now to trigger the problem</p>
   <p> WE need lot of garbage now to trigger the problem</p>
   <ent>&test1;</ent>
</doc>

