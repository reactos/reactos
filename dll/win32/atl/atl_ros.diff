Index: atl_main.c
===================================================================
--- atl_main.c	(revision 48273)
+++ atl_main.c	(working copy)
@@ -60,7 +60,7 @@
     INT i;
     UINT size;
 
-    FIXME("SEMI-STUB (%p %p %p)\n",pM,p,h);
+    //FIXME("SEMI-STUB (%p %p %p)\n",pM,p,h);
 
     size = pM->cbSize;
     switch (size)
